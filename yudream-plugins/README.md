# 迁移中的插件源码

这个目录当前包含两类内容：

1. `yudream-plugin-spi`  
   核心仓正式保留的插件契约模块

2. 业务插件源码  
   仍在工作区中保留，作为拆仓迁移缓冲和历史参考

注意：

- 根 `pom.xml` 已不再把业务插件模块纳入默认 reactor
- 核心 GitLab CI 也不再构建这些业务插件
- 运行时默认从根目录 `plugins/` 加载外部插件 JAR，而不是这里的 `target/`

后续应将业务插件迁移到独立插件仓。
