# 测试实例引导配置（IDEA 共享运行配置「yudream-test」经 YUDREAM_BOOTSTRAP_CONFIG 指向本文件）。
# 与开发实例共用同一台 Mongo 但库名不同（yudream-test）；首启自动播种默认管理员与菜单。
# Mongo 地址与开发环境不同时，改这里的 host:port 即可。
# 本文件存在即表示「已具备数据库配置」，测试实例不会误入安装向导切片。
spring.data.mongodb.uri=mongodb://localhost:27017/yudream-test
